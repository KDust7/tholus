{
  "name": "@uv-wasm/api-report",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "Apache-2.0 OR MIT",
  "description": "Freezes each package's public type surface so a breaking change has to be deliberate",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "report": "bun run build && node dist/cli.js",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "typescript": "~6.0.3",
    "vitest": "^4.1.10"
  }
}
