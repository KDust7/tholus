{
  "name": "@tholus/mock-engine",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "Apache-2.0 OR MIT",
  "description": "Protocol-faithful stand-in for the wasm engine, used to develop the SDK before the engine exists",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@tholus/engine-protocol": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "vitest": "^4.1.10"
  }
}
